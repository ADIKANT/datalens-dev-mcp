# Time-series template

Use for `line_chart`, `multiline_chart`, `area_completion`,
`vertical_bar_time_bucket`, `combo_time_series_combo`, and related time variants.
The template follows project-authored time-series validation patterns: grain-aware labels, denser y-grid,
top legend, direct endpoint labels, and safe `Editor.wrapFn` usage.
