# Category comparison template

Use for `horizontal_bar`, `grouped_bar`, `stacked_100`, `bullet_assignees`,
`heatmap`, and `waterfall` after the data contract is classified. Removed
lollipop, dumbbell, and plain stacked-bar requests resolve here.
