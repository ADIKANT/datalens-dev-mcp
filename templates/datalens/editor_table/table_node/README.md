# Table node template

Default route for exact-value tables, registries, source matrices, and bounded
JavaScript pivots. Grouped/sticky HTML and Advanced pivot recipes are
reference-only; use native pinned columns and nested `head.sub`. Pagination
defaults to 100 rows and accepts only integer values from 1 through 200.
