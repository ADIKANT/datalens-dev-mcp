# Requirements Templates

Copy or initialize these files into a project-local `requirements/` directory.
The MCP workflow updates them through `dl_init_requirements_workspace`,
`dl_ingest_requirements_markdown`, `dl_update_user_decision`, and
`dl_summarize_implementation_plan`.

