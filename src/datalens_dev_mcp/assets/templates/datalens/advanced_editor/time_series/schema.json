{
  "type": "object",
  "required": ["title", "rows"],
  "properties": {
    "title": {"type": "string"},
    "rows": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["bucket", "metric", "value"],
        "properties": {
          "bucket": {"type": "string"},
          "metric": {"type": "string"},
          "value": {"type": "number"}
        }
      }
    },
    "hint": {"type": "string"}
  }
}
