# Flow template

Use for `sankey_status_flow` only when source-target flow data is explicit.
This template is intentionally conservative; ordinary status counts route to
bars, tables, or funnel instead.
