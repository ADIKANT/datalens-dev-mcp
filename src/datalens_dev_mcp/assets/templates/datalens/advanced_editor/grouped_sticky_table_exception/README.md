# Grouped sticky table exception template

This template is reference-only and blocked by the current Advanced Editor
runtime contract because generated HTML table tags are not allowed. Do not route
creation to it. Use native `editor_table` / `table_node` with `head.sub`, pinned
columns, native links, and CSS cell states. Re-enable only after controlled-live
runtime evidence and an explicit policy change.
