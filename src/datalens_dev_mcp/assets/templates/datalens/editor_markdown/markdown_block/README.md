# Markdown block template

Use for section headers, owner/source notes, methodology, contacts, and links.
The compiler preserves explicit caller Markdown and blocks missing production
business content; only a section header may be derived from its supplied title.
Structured source rows belong in `table_node`, not Markdown.
