# Enhance Existing Synthetic Example

Use this lane when a dashboard already exists and the user asks to add a selector, table, or scoped widget.

Rules:

- hydrate a fresh baseline before update;
- preserve unknown dashboard fields;
- update only managed widgets and links;
- keep `mode: save`;
- require readback and deployment report.
