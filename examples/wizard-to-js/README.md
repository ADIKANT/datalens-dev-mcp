# Wizard Technology Preservation Synthetic Example

Existing Wizard charts remain Wizard by default. A route migration is explicit,
uses fresh saved readback, and must name the capability gap. Maps use
`wizard_native/geolayer`; ambiguous migrations become `manual_review_required`.
