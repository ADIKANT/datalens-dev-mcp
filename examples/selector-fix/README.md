# Selector Fix Synthetic Example

Selector repair changes only:

- selector defaults;
- affected widget params;
- aliases and links proved by fresh dashboard readback;
- local registry and deployment report.

It does not publish or alter unrelated widgets.
